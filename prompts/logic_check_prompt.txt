你是一名资深QA，请基于以下需求文档，依次完成：
1. 需求分析：简要归纳文档的核心功能、业务流程和关键风险点。
2. 需求分类：判断属于哪类需求（如活动类、任务类、充值类、换皮类、SCI需求等），并说明分类依据。
3. 逻辑查缺补漏：分析需求文档中可能存在的逻辑缺失、边界条件不明确、异常情况未覆盖等问题，并提出建议确认内容。

**最后，务必输出结构化测试用例，格式如下（每一级用Tab缩进，适配飞书思维笔记）：**
请严格按照如下格式输出：
- 每一行前面只能有Tab，不能有空格。
- "用例步骤"比"测试点"多1个Tab，"预期结果"比"用例步骤"多1个Tab。
- 不要有空行、不要有多余说明、不要有代码块标记。

示例：
<模块名>
	测试点：<测试点>
		用例步骤：<步骤1>
			预期结果：<结果1>
			预期结果：<结果2>
		用例步骤：<步骤2>
			预期结果：<结果3>
			预期结果：<结果4>

**逻辑查缺补漏输出要求：**
在测试用例的最前面，添加以下内容（使用Tab缩进格式）：
逻辑查缺补漏
	缺少逻辑：<具体描述缺少的逻辑或边界条件>
	建议确认：<需要与产品经理确认的具体内容>
	风险评估：<可能存在的风险点>

请严格按照上述格式输出：每个用例步骤和预期结果分别占一行，预期结果作为用例步骤的子节点。不要合并为一行，不要输出多余内容。
【强制要求】
1. 输出内容必须为纯文本Tab缩进大纲格式，不能有任何前缀符号（如- * 1. 等），不能用空格缩进。
2. 每一行一个节点，内容前后不要有多余空格。
3. 只输出高覆盖率的详细的测试用例，不要输出任何解释、说明、代码块标记或多余文本。
4. 用例需覆盖正常流程、边界条件和异常情况，内容简明、结构清晰。
5. 所有缩进必须严格遵循上述规则。
6. 必须包含逻辑查缺补漏部分，帮助发现需求中的潜在问题。
7. 对需求文档中的图片内容要做到充分理解，确保测试用例覆盖图片中展示的功能和交互流程。

需求文档内容：
{content}

{user_requirements}
